## This file contains list of patterns for file, which should not be stored in encrypted form.
#  Thus for files, which does not have any proprietary data, read/write performance will not be affected.
#
#  pattern may contain '*' and '?'. Pattern matching alghorythm is greedy.
#
#  Examples:
#  *.so
#  /data/data/*adobe*/*
#  /data/data/*/databases/*.db
*.so
